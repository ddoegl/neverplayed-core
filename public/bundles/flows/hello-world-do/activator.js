export default class Activator {
    start(_context) {
        // Purely declarative bundle - Orchestrator handles the rest
    }
    stop(_context) {
        // Cleanup handled by Orchestrator
    }
}
